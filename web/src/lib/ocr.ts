/* OCR for photos of paper letters.
 *
 * Wraps tesseract.js. Loads lazily.
 *
 * Stub for the scaffold. Real implementation in v0.1.
 */

export async function extractTextFromImage(_file: File): Promise<string> {
  return "";
}
